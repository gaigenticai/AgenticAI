# ===================================================================
# Agentic Platform Environment Configuration
# ===================================================================
# Copy this file to .env and update values as needed
# For production deployment, ensure all passwords are changed
# and secrets are properly managed
#
# IMPORTANT: Never commit the .env file to version control

# === AUTHENTICATION & SECURITY ===
# REQUIRE_AUTH controls whether authentication is enabled (Rule 10)
REQUIRE_AUTH=false

# JWT Configuration (for REQUIRE_AUTH=true)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRATION_HOURS=24


# OAuth2/OIDC Configuration (for REQUIRE_AUTH=true)
OAUTH_CLIENT_ID=your_oauth_client_id
OAUTH_CLIENT_SECRET=your_oauth_client_secret
OIDC_ISSUER_URL=https://your-oidc-provider.com

# === DATABASE CONFIGURATION ===
# Ingestion Layer Databases
POSTGRES_INGESTION_PASSWORD=agentic123
REDIS_INGESTION_PASSWORD=

# Database Connection URLs
DATABASE_URL=postgresql://agentic_user:agentic123@postgresql_ingestion:5432/agentic_ingestion

# Redis Configuration
REDIS_HOST=redis_ingestion
REDIS_PORT=6379

# MongoDB Configuration
MONGODB_INGESTION_PASSWORD=

# Output Layer Databases
POSTGRES_OUTPUT_PASSWORD=agentic123
MONGODB_PASSWORD=agentic123
TIMESCALEDB_PASSWORD=agentic123
NEO4J_PASSWORD=agentic123
ELASTICSEARCH_PASSWORD=

# === MESSAGE QUEUE & STREAMING ===
RABBITMQ_PASSWORD=agentic123
RABBITMQ_USER=agentic_user
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672

KAFKA_PASSWORD=
ZOOKEEPER_PASSWORD=

# === STORAGE & DATA LAKE ===
MINIO_USER=agentic_user
MINIO_PASSWORD=agentic123

# === MONITORING & LOGGING ===
GRAFANA_PASSWORD=admin123
FLUENTD_PASSWORD=

# === BACKUP & DISASTER RECOVERY ===
BACKUP_SCHEDULE=0 2 * * *
RETENTION_POLICY=30d

# === SERVICE PORTS ===
# Input Layer Services
INGESTION_COORDINATOR_PORT=8080
PORT=8080  # Default port for services (used by ingestion-coordinator)
CSV_INGESTION_PORT=8082

# Output Layer Services
OUTPUT_COORDINATOR_PORT=8081

# Vector Database Services
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334
QDRANT_HOST=qdrant_vector

# Vector UI Service
VECTOR_UI_PORT=8082
OUTPUT_COORDINATOR_URL=http://output-coordinator:8081

# Vector UI Authentication (for REQUIRE_AUTH=true)
AUTH_TOKEN=dev-auth-token-change-in-production

# Embedding Configuration
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
VECTOR_DIMENSIONS=384
PDF_INGESTION_PORT=8083
EXCEL_INGESTION_PORT=8084
JSON_INGESTION_PORT=8085
API_INGESTION_PORT=8086
UI_SCRAPER_PORT=8087

# Output Layer Services
OUTPUT_COORDINATOR_PORT=8081
POSTGRES_OUTPUT_PORT=5433
MONGODB_PORT=27017
QDRANT_HTTP_PORT=6333
QDRANT_GRPC_PORT=6334
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_TCP_PORT=9300
TIMESCALEDB_PORT=5434
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687
MINIO_BRONZE_API_PORT=9000
MINIO_BRONZE_CONSOLE_PORT=9001
MINIO_SILVER_API_PORT=9010
MINIO_SILVER_CONSOLE_PORT=9002
MINIO_GOLD_API_PORT=9020
MINIO_GOLD_CONSOLE_PORT=9003

# Infrastructure Services
POSTGRES_INGESTION_PORT=5432
REDIS_INGESTION_PORT=6379
RABBITMQ_PORT=5672
RABBITMQ_MANAGEMENT_PORT=15672
ZOOKEEPER_PORT=2181
KAFKA_PORT=9092

# Monitoring Services
PROMETHEUS_PORT=9090
GRAFANA_PORT=3000
JAEGER_QUERY_PORT=16686
JAEGER_COLLECTOR_ZIPKIN_PORT=9411
JAEGER_COLLECTOR_OTLP_PORT=4318
FLUENTD_PORT=24224

# Security Services
OAUTH2_PROXY_PORT=4180

# === SERVICE CONFIGURATION ===
# Database Connection Pooling
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20
DB_POOL_TIMEOUT=30

# Redis Configuration
REDIS_MAX_CONNECTIONS=20
REDIS_CONNECTION_TIMEOUT=5

# Message Queue Configuration
RABBITMQ_PREFETCH_COUNT=10
RABBITMQ_HEARTBEAT=60

# Kafka Configuration
KAFKA_NUM_PARTITIONS=3
KAFKA_REPLICATION_FACTOR=1
KAFKA_RETENTION_HOURS=168

# Elasticsearch Configuration
ELASTICSEARCH_CLUSTER_NAME=agentic-cluster
ELASTICSEARCH_NODE_NAME=agentic-node-1

# Qdrant Configuration
QDRANT_COLLECTION_SIZE=10000
QDRANT_VECTOR_SIZE=768
QDRANT_DISTANCE_METRIC=cosine

# MinIO Configuration
MINIO_REGION=us-east-1
MINIO_BROWSER=off

# Data Lake Service Configuration
DATA_LAKE_PORT=8090
MINIO_ENDPOINT_BRONZE=http://minio_bronze:9000
MINIO_ENDPOINT_SILVER=http://minio_silver:9010
MINIO_ENDPOINT_GOLD=http://minio_gold:9020

# Message Queue Service Configuration
MESSAGE_QUEUE_PORT=8091

# Redis Caching Service Configuration
REDIS_CACHING_PORT=8092

# OAuth2/OIDC Service Configuration
OAUTH2_OIDC_PORT=8093
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7
BASE_URL=http://localhost:8093

# Data Encryption Service Configuration
DATA_ENCRYPTION_PORT=8094

# Monitoring Service Configuration
MONITORING_PORT=8095
GRAFANA_API_KEY=admin:admin

# Audit Compliance Service Configuration
AUDIT_COMPLIANCE_PORT=8096

# Backup Orchestration Service Configuration
BACKUP_ORCHESTRATION_PORT=8097
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_REGION=us-east-1

# Port Manager Service Configuration
PORT_MANAGER_PORT=8098

# Jaeger Distributed Tracing Configuration
JAEGER_UI_PORT=16686
JAEGER_COLLECTOR_PORT=16688
JAEGER_AGENT_PORT=6831
JAEGER_ENDPOINT=http://jaeger:16686

# Distributed Tracing Service Configuration
TRACING_JAEGER_PORT=8099

# GraphQL API Service Configuration
GRAPHQL_API_PORT=8100

# === LOGGING & MONITORING ===
LOG_LEVEL=INFO
LOG_FORMAT=json
METRICS_INTERVAL=15s
HEALTH_CHECK_INTERVAL=30s

# Alerting Thresholds
CPU_THRESHOLD=80
MEMORY_THRESHOLD=85
DISK_THRESHOLD=90
ERROR_RATE_THRESHOLD=5

# === BACKUP CONFIGURATION ===
BACKUP_RETENTION_DAYS=30
BACKUP_COMPRESSION=gzip
BACKUP_ENCRYPTION=true

# === NETWORKING ===
DOCKER_NETWORK=agentic-network
NETWORK_SUBNET=172.20.0.0/16
NETWORK_GATEWAY=172.20.0.1

# === DEVELOPMENT SETTINGS ===
DEBUG_MODE=false
ENABLE_SWAGGER=true
ENABLE_METRICS=true
ENABLE_TRACING=true

# === TLS/SSL CONFIGURATION ===
SSL_CERT_PATH=/etc/ssl/certs/agentic.crt
SSL_KEY_PATH=/etc/ssl/private/agentic.key
SSL_CA_CERT_PATH=/etc/ssl/certs/ca.crt

# === RESOURCE LIMITS ===
# Memory limits (in MB)
INGESTION_COORDINATOR_MEMORY=512
OUTPUT_COORDINATOR_MEMORY=512
DATABASE_MEMORY=1024
CACHE_MEMORY=256
MESSAGE_QUEUE_MEMORY=512

# CPU limits
INGESTION_COORDINATOR_CPU=0.5
OUTPUT_COORDINATOR_CPU=0.5
DATABASE_CPU=1.0
CACHE_CPU=0.25
MESSAGE_QUEUE_CPU=0.5

# === AUTO-SCALING ===
ENABLE_AUTO_SCALING=false
MIN_REPLICAS=1
MAX_REPLICAS=5
CPU_SCALE_THRESHOLD=70
MEMORY_SCALE_THRESHOLD=75

# === DATA QUALITY & VALIDATION ===
ENABLE_DATA_VALIDATION=true
VALIDATION_STRICT_MODE=false
SCHEMA_DRIFT_DETECTION=true
DUPLICATE_DETECTION=true
DATA_PROFILING_ENABLED=true

# === PERFORMANCE TUNING ===
CONNECTION_POOL_SIZE=20
REQUEST_TIMEOUT=30
MAX_CONCURRENT_REQUESTS=100
RATE_LIMIT_REQUESTS=1000
RATE_LIMIT_WINDOW=60

# === FEATURE FLAGS ===
ENABLE_STREAMING_INGESTION=true
ENABLE_REAL_TIME_PROCESSING=true
ENABLE_ADVANCED_ANALYTICS=false
ENABLE_AI_INSIGHTS=false
ENABLE_CUSTOM_TRANSFORMATIONS=true

# === EXTERNAL INTEGRATIONS ===
ENABLE_SLACK_NOTIFICATIONS=false
SLACK_WEBHOOK_URL=
ENABLE_EMAIL_NOTIFICATIONS=false
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# === COMPLIANCE & AUDIT ===
AUDIT_LOG_RETENTION=365
COMPLIANCE_MODE=standard
PII_DETECTION_ENABLED=true
DATA_CLASSIFICATION_ENABLED=true

# === DISASTER RECOVERY ===
DR_REPLICATION_ENABLED=false
DR_REPLICATION_REGION=
DR_BACKUP_SCHEDULE=0 3 * * *
DR_FAILOVER_TIMEOUT=300

# === COST OPTIMIZATION ===
ENABLE_COST_OPTIMIZATION=false
STORAGE_CLASS=standard
COMPUTE_OPTIMIZATION=balanced

# === ADVANCED CONFIGURATION ===
# Custom environment variables can be added below
# CUSTOM_VAR1=value1
# CUSTOM_VAR2=value2

# === AGENTIC BRAIN CONFIGURATION ===
# Agent Orchestrator Service
AGENT_ORCHESTRATOR_PORT=8200
AGENT_ORCHESTRATOR_HOST=0.0.0.0

# Plugin Registry Service
PLUGIN_REGISTRY_PORT=8201
PLUGIN_REGISTRY_HOST=0.0.0.0

# Workflow Engine Service
WORKFLOW_ENGINE_PORT=8202
WORKFLOW_ENGINE_HOST=0.0.0.0

# Template Store Service
TEMPLATE_STORE_PORT=8203
TEMPLATE_STORE_HOST=0.0.0.0

# Rule Engine Service
RULE_ENGINE_PORT=8204
RULE_ENGINE_HOST=0.0.0.0

# Memory Manager Service
MEMORY_MANAGER_PORT=8205
MEMORY_MANAGER_HOST=0.0.0.0

# Agent Builder UI Service
AGENT_BUILDER_UI_PORT=8300
AGENT_BUILDER_UI_HOST=0.0.0.0

# UI-to-Brain Mapper Service
UI_TO_BRAIN_MAPPER_PORT=8302
UI_TO_BRAIN_MAPPER_HOST=0.0.0.0

# Reasoning Module Factory Service
REASONING_MODULE_FACTORY_PORT=8304
REASONING_MODULE_FACTORY_HOST=0.0.0.0

# Brain Factory Service
BRAIN_FACTORY_PORT=8301
BRAIN_FACTORY_HOST=0.0.0.0

# Agent Brain Base Class Service
AGENT_BRAIN_BASE_PORT=8305
AGENT_BRAIN_BASE_HOST=0.0.0.0

# Service Connector Factory Service
SERVICE_CONNECTOR_FACTORY_PORT=8306
SERVICE_CONNECTOR_FACTORY_HOST=0.0.0.0

# Deployment Pipeline Service
DEPLOYMENT_PIPELINE_PORT=8303
DEPLOYMENT_PIPELINE_HOST=0.0.0.0

# UI Testing Service
UI_TESTING_PORT=8310
UI_TESTING_HOST=0.0.0.0

# UI Testing Configuration
DEFAULT_BROWSER=chrome
HEADLESS_MODE=true
TEST_TIMEOUT=30
SCREENSHOT_ON_FAILURE=true
PAGE_LOAD_THRESHOLD_MS=3000
INTERACTION_RESPONSE_THRESHOLD_MS=1000
MEMORY_USAGE_THRESHOLD_MB=500

# Integration Tests Configuration
INTEGRATION_TESTS_PORT=8320
INTEGRATION_TESTS_HOST=0.0.0.0
DEFAULT_TEST_TIMEOUT=300
MAX_CONCURRENT_TESTS=5
RETRY_FAILED_TESTS=true
MAX_RETRIES=3
MAX_RESPONSE_TIME_MS=5000
MIN_SUCCESS_RATE=0.95

# Authentication Service Configuration
AUTH_SERVICE_PORT=8330
AUTH_SERVICE_HOST=0.0.0.0

# JWT Configuration
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# Password Security
PASSWORD_MIN_LENGTH=8
PASSWORD_REQUIRE_UPPERCASE=true
PASSWORD_REQUIRE_LOWERCASE=true
PASSWORD_REQUIRE_DIGITS=true
PASSWORD_REQUIRE_SPECIAL=false

# Session Management
SESSION_TIMEOUT_MINUTES=60
MAX_LOGIN_ATTEMPTS=5
ACCOUNT_LOCK_DURATION_MINUTES=30

# MFA Settings
MFA_ISSUER_NAME=Agentic Brain

# OAuth2 Configuration
OAUTH2_ENABLED=false
OAUTH2_GOOGLE_CLIENT_ID=your-google-client-id
OAUTH2_GOOGLE_CLIENT_SECRET=your-google-client-secret
OAUTH2_GITHUB_CLIENT_ID=your-github-client-id
OAUTH2_GITHUB_CLIENT_SECRET=your-github-client-secret

# Email Configuration
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_FROM=noreply@agenticbrain.com

# Security Configuration
ENCRYPTION_KEY=your-32-byte-encryption-key
SECURITY_HEADERS_ENABLED=true
CORS_ORIGINS=http://localhost:8300,http://localhost:3000

# Audit Logging Service Configuration
AUDIT_SERVICE_PORT=8340
AUDIT_SERVICE_HOST=0.0.0.0

# Elasticsearch Configuration (Optional)
ELASTICSEARCH_ENABLED=false
ELASTICSEARCH_HOST=elasticsearch
ELASTICSEARCH_PORT=9200
ELASTICSEARCH_INDEX=audit_events

# Audit Configuration
MAX_EVENTS_PER_REQUEST=100
AUDIT_QUEUE_SIZE=10000
BATCH_SIZE=50

# Data Retention Policies
DEFAULT_RETENTION_DAYS=365
ARCHIVE_AFTER_DAYS=90
DELETE_AFTER_DAYS=2555

# Compliance Settings
GDPR_ENABLED=true
SOX_ENABLED=true
HIPAA_ENABLED=false
PCI_ENABLED=false

# Alert Configuration
ALERT_ENABLED=true
ALERT_EMAIL_ENABLED=false
ALERT_WEBHOOK_ENABLED=false

# Audit Security
API_KEY_REQUIRED=true
RATE_LIMIT_ENABLED=true
MAX_REQUESTS_PER_MINUTE=1000

# Monitoring Metrics Service Configuration
METRICS_SERVICE_PORT=8350
METRICS_SERVICE_HOST=0.0.0.0

# Metrics Collection
COLLECTION_INTERVAL_SECONDS=30
METRICS_RETENTION_DAYS=90
ENABLE_HISTORICAL_STORAGE=true

# Alert Configuration
ALERT_ENABLED=true
ALERT_EMAIL_ENABLED=false
ALERT_WEBHOOK_ENABLED=false

# Performance Monitoring
ENABLE_PERFORMANCE_BASELINES=true
BASELINE_CALCULATION_DAYS=30

# Grafana Dashboards Service Configuration
GRAFANA_DASHBOARDS_PORT=8360
GRAFANA_DASHBOARDS_HOST=0.0.0.0

# Grafana Configuration
GRAFANA_URL=http://grafana:3000
GRAFANA_API_KEY=your_grafana_api_key
GRAFANA_USERNAME=admin
GRAFANA_PASSWORD=admin

# Prometheus Configuration
PROMETHEUS_URL=http://prometheus:9090

# Dashboard Configuration
DASHBOARD_REFRESH_INTERVAL=30
ENABLE_AUTO_PROVISIONING=true
DASHBOARD_RETENTION_DAYS=90

# Error Handling Service Configuration
ERROR_HANDLING_PORT=8370
ERROR_HANDLING_HOST=0.0.0.0

# Error Handling Configuration
ENABLE_AUTO_RECOVERY=true
ERROR_RETENTION_DAYS=90
MAX_RECOVERY_ATTEMPTS=3
RECOVERY_TIMEOUT_SECONDS=300

# Error Alert Configuration
ENABLE_ERROR_ALERTS=true
CRITICAL_ERROR_THRESHOLD=10
ERROR_RATE_ALERT_THRESHOLD=0.1

# End-to-End Testing Service Configuration
E2E_TESTING_PORT=8380
E2E_TESTING_HOST=0.0.0.0

# Test Configuration
MAX_CONCURRENT_TESTS=5
TEST_TIMEOUT_SECONDS=300
CLEANUP_AFTER_TEST=true

# UI Testing Configuration
SELENIUM_HUB_URL=http://localhost:4444/wd/hub
UI_TEST_TIMEOUT=60
BROWSER_HEADLESS=true

# Load Testing Configuration
LOAD_TEST_USERS=10
LOAD_TEST_DURATION=60
LOAD_TEST_RAMP_UP=10

# Test Results Configuration
ENABLE_METRICS=true
TEST_RESULTS_RETENTION_DAYS=30

# LLM Configuration for Agentic Brain
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
DEFAULT_LLM_MODEL=gpt-4
DEFAULT_LLM_TEMPERATURE=0.7
DEFAULT_LLM_MAX_TOKENS=2000

# Agent Memory Configuration
WORKING_MEMORY_TTL_SECONDS=3600
EPISODIC_MEMORY_RETENTION_DAYS=30
SEMANTIC_MEMORY_ENABLED=true

# Reasoning Pattern Configuration
REACT_MAX_STEPS=10
REFLECTION_MAX_ITERATIONS=5
PLANNING_MAX_DEPTH=5
MULTI_AGENT_MAX_AGENTS=5

# Agent Execution Configuration
DEFAULT_EXECUTION_TIMEOUT=300
MAX_CONCURRENT_TASKS=10
MEMORY_TTL_SECONDS=3600

# Performance Monitoring
ENABLE_METRICS=true
METRICS_RETENTION_HOURS=24

# Service Connector Configuration
MAX_CONNECTIONS_PER_SERVICE=10
CONNECTION_TIMEOUT=30
CONNECTION_RETRY_ATTEMPTS=3
CONNECTION_RETRY_DELAY=1.0

# Health Monitoring
HEALTH_CHECK_INTERVAL=30
SERVICE_TIMEOUT_THRESHOLD=60
LONG_TERM_MEMORY_ENABLED=true
MEMORY_VECTOR_DIMENSION=768
MEMORY_SIMILARITY_THRESHOLD=0.85

# Agent Plugin Configuration
PLUGIN_LOAD_TIMEOUT_SECONDS=30
PLUGIN_EXECUTION_TIMEOUT_SECONDS=300
DOMAIN_PLUGINS_ENABLED=true
GENERIC_PLUGINS_ENABLED=true
PLUGIN_AUTO_UPDATE_ENABLED=false

# Agent Reasoning Configuration
REACT_MAX_STEPS=10
REFLECTION_MAX_ITERATIONS=5
PLANNING_HORIZON_DAYS=30
MULTI_AGENT_MAX_AGENTS=5
REASONING_TIMEOUT_SECONDS=600

# Agent Deployment Configuration
DEPLOYMENT_VALIDATION_ENABLED=true
DEPLOYMENT_TEST_TASK_TIMEOUT=60
DEPLOYMENT_ROLLBACK_ENABLED=true
DEPLOYMENT_HEALTH_CHECK_INTERVAL=30
MAX_CONCURRENT_AGENT_SESSIONS=10

# Agent Monitoring and Metrics
AGENT_METRICS_ENABLED=true
AGENT_METRICS_RETENTION_DAYS=90
AGENT_PERFORMANCE_MONITORING=true
AGENT_ERROR_TRACKING_ENABLED=true
AGENT_AUDIT_LOG_ENABLED=true

# Agent Template Configuration
TEMPLATE_CACHE_ENABLED=true
TEMPLATE_CACHE_TTL_SECONDS=3600
TEMPLATE_VERSION_CONTROL_ENABLED=true
TEMPLATE_AUTO_BACKUP_ENABLED=true

# Agent Workflow Configuration
WORKFLOW_MAX_COMPONENTS=50
WORKFLOW_EXECUTION_TIMEOUT=1800
WORKFLOW_PARALLEL_EXECUTION=true
WORKFLOW_ERROR_RECOVERY_ENABLED=true

# Agent Security Configuration
AGENT_ENCRYPTION_ENABLED=true
AGENT_SESSION_TIMEOUT_MINUTES=60
AGENT_API_RATE_LIMIT=100
AGENT_API_RATE_LIMIT_WINDOW=60

# === SERVICE-SPECIFIC CONFIGURATION ===
# Ingestion Coordinator Configuration
INGESTION_COORDINATOR_HOST=0.0.0.0

# Output Coordinator Configuration
OUTPUT_COORDINATOR_HOST=0.0.0.0

# Vector UI Configuration
VECTOR_UI_HOST=0.0.0.0

# === DOCKER & CONTAINER CONFIGURATION ===
# Docker Compose Project Name
COMPOSE_PROJECT_NAME=agentic-platform

# Docker Network Configuration
DOCKER_NETWORK_NAME=agentic-network

# === LOGGING CONFIGURATION ===
# Structured logging format (json, logfmt, plain)
LOG_FORMAT=json

# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Enable console logging
ENABLE_CONSOLE_LOGGING=true

# === DEVELOPMENT & DEBUGGING ===
# Enable debug mode (adds detailed error messages and stack traces)
DEBUG_MODE=false

# Enable request/response logging
ENABLE_REQUEST_LOGGING=false

# Enable SQL query logging (WARNING: May log sensitive data)
ENABLE_SQL_LOGGING=false

# === PRODUCTION CHECKLIST ===
# Before deploying to production, ensure:
# 1. All default passwords have been changed
# 2. JWT_SECRET is a strong, randomly generated secret
# 3. SSL/TLS certificates are configured for HTTPS
# 4. Database backups are configured and tested
# 5. Monitoring and alerting are set up
# 6. Rate limiting is configured for API endpoints
# 7. CORS settings are appropriate for your domain
# 8. Session timeouts are configured appropriately
# 9. Audit logging is enabled and monitored
# 10. Regular security updates are scheduled
